<?php

return [
    'locale' => env('APP_LOCALE', 'en'), // Idioma por defecto desde .env o 'en' si no está definido
    'fallback_locale' => env('APP_FALLBACK_LOCALE', 'en'), // Idioma de reserva desde .env o 'en' si no está definido
];
